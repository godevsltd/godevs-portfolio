<?php
/**
 * Silence is golden.
 *
 * This file is required for WordPress to recognise the theme, but the actual
 * front-end rendering for this block theme happens through the block templates
 * in `templates/` and template parts in `parts/`. If a template cannot be
 * located, WordPress falls back to `templates/index.html`.
 *
 * @package GoDevs_Portfolio
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}
