/**
 * GoDevs Portfolio — Progressive enhancement JavaScript.
 *
 * Phase 1 ships NO JavaScript functionality. This file exists as a placeholder
 * for future progressive enhancement only. It is NOT enqueued by functions.php
 * until it contains actual code.
 *
 * When adding JS:
 *   - Confirm the enhancement degrades gracefully without JS.
 *   - Confirm vanilla JS only (no jQuery, no external libs).
 *   - Enqueue in functions.php with `strategy: 'defer'` and `in_footer: true`.
 *   - See docs/PERFORMANCE.md and docs/SECURITY.md for the rules.
 *
 * @package GoDevs_Portfolio
 */

/* Intentionally empty for Phase 1. */
