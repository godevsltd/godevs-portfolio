# This directory holds theme-owned images (icons, default Open Graph image,
# favicon, etc.). v0.1 ships this directory intentionally empty.
#
# The theme does NOT ship default Open Graph images, favicons, or site
# icons — users provide these via the Site Editor (logo, site icon) and
# the WordPress Customizer (site icon) respectively.
#
# Future phases may add:
#   - A default Open Graph image (og-default.png)
#   - A default favicon (favicon.svg + favicon.ico)
#   - Theme-native SVG icons (icons/*.svg) if a custom icon set is needed
#
# Do NOT add:
#   - Random stock photos pulled from Google Images
#   - AI-generated illustrations or icons
#   - Fake team photos or office images
#   - Unlicensed imagery of any kind
